hubot {
  name = "scalabot"

  listeners = ["org.dberg.hubot.models.TestListener", "org.dberg.hubot.models.TestListener2", "org.dberg.hubot.models.HelpListener"]

  middleware = []

  adapter = "org.dberg.hubot.adapter.ShellAdapter"

  brain = "mapdb"
  brainFile = "/tmp/brain.db"

}

hipchat {
  jid="111@chat.hipchat.com"
  password="somePass"
  chatAlias = "ScalaBot" //cant be same as name, need to read XEP to see why 
}
